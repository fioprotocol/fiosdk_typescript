#Generate FIO Private/Public Keys using a mnemonic.

	Change the mnemonic variable in the index.js file, to reflect the mnemonic you wish to use for key generation.

## Install the FIOSDK dependencies
npm install

##Run the command
node index.js